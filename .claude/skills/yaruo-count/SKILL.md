---
name: yaruo-count
description: やる夫式教材（docs/books/*.md）の文字数・行数・UTF-8バイト数と推定読了時間を、話者表示・カギカッコ・全角空白・TeX数式を考慮した統一ルールで集計するスキル。ユーザーが教材の文字数、行数、ファイルサイズ、数式量、読了時間、読む時間、所要時間の計算や再集計を依頼した場合、または他の生成処理へ読了時間を組み込む場合は必ず使うこと。
---

# やる夫教材カウント

集計規則の正本をこのスキルに置き、手計算や別実装を避ける。

## 集計する

リポジトリルートで次を実行する。

```bash
python3 .claude/skills/yaruo-count/scripts/count_textbooks.py
```

特定ファイルだけを集計する場合はパスを渡す。

```bash
python3 .claude/skills/yaruo-count/scripts/count_textbooks.py docs/books/example.md
```

機械処理には `--format json` を使う。別のPythonスクリプトから利用するときは、同スクリプトの `count_document(path)` を呼び出す。

## 文字数・行数の規則

- バイト数は元ファイル全体のUTF-8バイト数とする。
- 文字数はUnicodeコードポイント数とし、改行コードを含めない。
- `**やる夫**：` および `**やらない夫**：` だけからなる話者表示行を、文字数・行数の両方から除外する。
- セリフ本文は集計対象に含める。ただし、セリフを囲む外側の `「`・`」` と、セリフ各行の冒頭にある全角空白（U+3000）は文字数から除外する。
- 行数は話者表示行を除外した後の物理行数とし、空行を含める。カギカッコや全角空白だけを除外しても行自体は残す。

## 読了時間の規則

- Markdownの `$...$` と `$$...$$` を、区切り記号を含めてTeX数式部分とする。
- コードフェンスとインラインコード内の `$` は数式として扱わない。
- TeX数式部分は10 Unicodeコードポイント/秒、それ以外の会話・本文部分は40 Unicodeコードポイント/秒として計算する。
- 両者の秒数を合計した後、分単位で四捨五入する。表示は `(0分)` 形式とする。

## 検証する

変更後は代表的な教材と全文書の両方でスクリプトを実行し、次を確認する。

- `math_characters + other_characters == characters`
- 秒数・分数が負でない
- 同じ入力から同じ結果が得られる
