---
name: yaruo-format
description: やる夫式教材（docs/books/*/README.md）の公開前に、レンダリングが崩れる書式を機械的に検出・修正するlintスキル。日本語の約物と隣接して壊れる強調記号、区切り行の無い表、発言外枠のカギカッコ、発言末の句点を scripts/yaruo_lint.py で一括修正する。「整形」「フォーマット」「lint」「太字が効かない」「強調が崩れる」「表が崩れる」「カギカッコ」「句点」に言及し、対象がやる夫式教材の .md なら使う。
---

# やる夫式教材 書式整形スキル

対象は `yaruo-rediscovery` が生成した会話形式の教材（`docs/books/*/README.md`）。これは**公開前の機械lint**であり、書式の予防そのものは `yaruo-rediscovery` の「Markdown書式」指定が担う。

主眼は、執筆時の指定を守っても混入する CommonMark/GFM のレンダリング崩れ（`emphasis-flanking`・`table-delimiter`）にある。残る2つは保険。

**すべてスクリプトで機械的に行う。手作業でやらない。** モデルが本文を読み直して書き直すより、バイト単位で修正するこの機械lintのほうが安く確実である。

## 手順

```bash
python3 scripts/yaruo_lint.py docs/books/<id>/README.md --check   # 検出だけ
python3 scripts/yaruo_lint.py docs/books/<id>/README.md --fix     # 適用
python3 scripts/yaruo_lint.py docs/books/<id>/README.md --check   # 0件を確認
```

ルールの適用順は `--fix` の内部で固定されている（会話表記を確定させてから強調・表・句点）。個別に回したいときだけ `--rules <id>` を使う。`--list-rules` で一覧。

| ルールID | 直すもの |
|---|---|
| `dialogue-frame` | 発言外枠のカギカッコ、行頭の全角空白 |
| `emphasis-flanking` | 約物に隣接して壊れた `**` |
| `table-delimiter` | GFM 表の区切り行、表の行頭空白 |
| `dialogue-period` | 発言末の句点 |

1. `--check` を実行する。
2. error があれば `--fix` を適用する。
3. 再度 `--check` を実行し、error が0件になることを確認する。
4. **warning は目視で判断する**（下記）。機械的に0を目指さない。
5. `git diff --stat` の変更行数がスクリプトの報告と整合することを確認する。

## warning への対応

自動修正せず報告するものは、該当行を目視して手で直す。

| warning | 対応 |
|---|---|
| 壊れた発言外枠（対応する `」` が無い） | 先頭の `「` だけ削除済み。閉じ位置を手で確認する |
| `**` の直後（直前）が空白 | `** 重要**` 型の書き間違い。空白を内側から外側へ移す |
| 区切り行の列数がヘッダと不一致 | GFM では表にならない。区切り行の列数をヘッダに合わせる |
| 表の直前が空行でない | 段落の続きと解釈される。空行を1行入れる |
| 直前の行に `|` が含まれる | ヘッダ行が別の文と融合している可能性。そのブロックは自動修正していない |
| 読点・コロン・セミコロンで終わる発言 | 句点を自動補完していない。文が途中で切れていないか確認する |

## 補足（なぜ壊れるか）

- **`emphasis-flanking`**：CommonMark の flanking 規則により、`**` の外側が文字で内側が約物（`。「」（）『』：` など）に接していると強調として解釈されず、`**` がそのまま表示される。日本語はスペースを入れずに書くため頻発する。壊れている側の外側へ半角スペースを1つ挿入し、正常な箇所には触れない。
- **`table-delimiter`**：GFM の表はヘッダ行の直後に区切り行が必須。`|` で始まる行が2行以上連続するブロックを表とみなし、2行目が区切り行でなければヘッダの列数に合わせて挿入する。1行だけの `|` は本文の可能性があるため触らない。
- コードブロック・インラインコード・TeX数式内はいずれのルールも対象外。

## 回帰

ルールを変更したら必ず実行する。fixture は `scripts/tests/fixtures/<ルールID>/` に input と expected の対で置く。

```bash
python3 scripts/tests/run_lint_tests.py
```

## 完了報告

ファイルごとに、ルール別の修正件数、warning の一覧と各々への対応（手で直した／問題なしと判断した）、`git diff --stat` との整合を報告する。

## チェックリスト

- [ ] `--check` の再実行で error が0件になったか
- [ ] warning をすべて目視確認したか
- [ ] `git diff` に意図しない変更（本文の書き換え等）が混ざっていないか
