.claude/settings.local.json

# Generated by scripts/generate_site.py; see scripts/site_template.html for the
# source shell and docs/catalog.yaml for the source catalog.
#
# docs/README.md is deliberately NOT listed here. Only its catalog block
# (between the BEGIN/END GENERATED CATALOG markers) is generated; the surrounding
# introduction is hand-written, and generate_site.py reads the existing file to
# rewrite that block in place. Untracking it makes the build fail on a fresh clone.
/docs/_sidebar.md
/docs/index.html
/docs/404.html
/docs/books/*/index.html
/docs/sitemap.xml
