# プレビュー上のコメント

開発サーバ（`scripts/dev_server.py`）のプレビューで本文を選んでコメントを付け、常駐したエージェントがその場で直す経路がある。GitHub の PR にコメントして直してもらうのと同じ流れを、ローカルで完結させるためのもの。

## 使い方

教材ページを開くと常にコメントモードで、右側にパネルが出る（開発サーバ経由のときだけ。本番サイトには入らない）。

- 本文を**なぞって選ぶ**（普通のテキスト選択）。選んだ範囲から単位（文・発言・節）は自動で決まる。Esc で解除。
- 選択はコメントの**対象**であって修正の範囲ではない。全体に関わる指摘を一文だけ選んで書いてよく、どこまで直すかは対応する側がコメントの内容で判断する。
- 右側のパネルは常に開いていて、上に履歴（古い順）、最下段が入力欄。`Ctrl+Enter` で送信。行はコメントの先頭だけを表示し、クリックすると指摘と対応の全履歴が開く。対応済みの結果へ追加コメントを送り、同じコメントを再び処理中へ戻せる。
- 「軽微／内容」の別は、対応するエージェントへ最初に渡す本文の量（段落の前後だけか、節ぜんぶか）を決める。足りなければ対応側が `--scope` で引き直す。
- コメントをクリックすると対象箇所へ移動する。引用が改稿済みなら、最後に記録した現在行、その行も特定できなければ現在見出しの順にフォールバックする。
- パネル上部に、コメントを拾う担当の状態（待機中／対応中／未接続）と、担当が名乗ったモデル・effort が出る。未接続のときは、その下に起動方法が出る。
- 件数はステータスのアイコン（⏳処理中 ✔対応済み ⚠位置不明 ☑確認済み）ごとに並び、アイコンを押すとその状態の表示を切り替えられる。確認済みは既定で畳んである。

コメントの一覧・対応・状態変更は CLI からも行える：

```bash
python3 scripts/comments.py list --book {ID}
python3 scripts/comments.py show --book {ID} --id 0007
python3 scripts/comments.py answer --book {ID} --id 0007 --message '…'
```

## ファイルと責務

- コメントは `comments/{ID}/{連番}.md`（OKF v0.1：YAML frontmatter 付き Markdown、`type: comment`）。`docs/` の外に置くのは、`generate_site.py` が `docs/` を丸ごと `build/` へコピーするため。`.gitignore` 済みでサイトには公開されない。
- 永続化・アンカー解決・窓の切り出しは `scripts/comments.py` が唯一の入口。`dev_server.py` は HTTP 層だけを持つ。

## アンカー

- **アンカーの正本は「見出し＋引用文字列＋節内の出現順」**。行番号は編集のたびに陳腐化するので `line_hint` は投稿時の目安にすぎず、`comments.py` が毎回引き直す。再解決に成功した位置は `current_line`・`current_heading`・`located_at` として同じコメントへ記録し、失敗時は最後の成功位置を維持する。引用が見つからなければ `stale` として人へ差し戻し、似た別の場所へは決して落とさない。
- 引用の突き合わせは `comments.normalize()` の正規形で行う（ルビの読み・強調記号・リンク URL・数式・空白を落とす）。ブラウザ側の同じ処理は `scripts/dev_comment_ui.js` にあり、**両者は対で保つ**。

## 状態と対応

- 状態は `open`（処理中）→ `answered`（AI が対応）→ `resolved`（人が確認）。対応結果へ人が追加コメントすると、同じファイルへ履歴を追記して `open` に戻る。**`resolved` にできるのは人だけ**で、パネルの「確認して閉じる」を押す。
- 選択範囲はコメントの**対象**を示すもので、修正範囲ではない。どこまで直すかは対応する側がコメントの内容で判断し、必要なら `comments.py show --scope section|part` で窓を引き直す。
- 対応する側の作法は `/comment-eater`（または `$comment-eater`）。全文を読み直さず、`comments.py wait` が渡す窓だけで直す。`python3 scripts/comments.py wait --session … --model … --effort …` で全教材を待ち受け、GET の応答で対象教材を受け取る。待機の GET でモデルと effort を名乗り、その接続を掴んでいる間が「待機中」。担当は1教材に1つで、二人目は終了コード 2（判定は dev_server がメモリで持つ）。

## テスト

変更したら `python3 scripts/tests/run_comment_tests.py`。
