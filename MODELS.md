# 工程ごとのモデルと effort

**一つの「最強モデル」を決めない。** 高価なモデルは、品質差が実際に出る工程へ投入する。
どの工程で差が出るかは測って決める（`bench/README.md`）。

このファイルは**仮説と実測を分けて書く。** 仮説のまま運用してよいが、仮説を実測として引用しない。

## 工程の重さ（仮説）

**一回の生成で全部をやらせない。** 下の表は工程の一覧を兼ねており、上から順に進む。特に、正確性の検証・教育性のレビュー・物語性のレビューは**別々の工程として分ける**（一度の総合判断に混ぜると、面白さが欠落を埋め合わせてしまう）。

| 工程 | 重さ | effort の目安 | 差が出ると見ている理由 |
|---|---|---|---|
| 問い・学習目標を決める | 重 | high | 出発点を間違えると以降すべてが無駄になる |
| 幕構成の設計（プロット） | 重 | high〜xhigh（要検証） | 複数の制約を同時に満たす配置。やり直しが最も高くつく |
| 素朴案・反例の設計 | 重 | high | 藁人形と「本当に思いつく案」の差はここで決まる |
| 本文執筆 | 重 | medium〜high | 制約を守りながら会話にする。effort 差が読み味に出るかは未検証 |
| 正確性検証・出典 | 中 | medium | 手順（`yaruo-proofread` の段構え）が効く。モデル差より手順差が大きい仮説 |
| 教育性レビュー | 重 | high | 「何が分かりにくいか」の発見は長文横断 |
| 物語性レビュー | 重 | high | 同型反復は語ではなく型の一致で見る。表層一致では捕まらない |
| 前提知識の破綻の発見 | 重 | high | 定義前使用は遠く離れた2点の照合 |
| 図版の選定（どこに図が要るか） | 重 | high | 理解の障害を本文から見つける |
| 図版の実装（SVG・プロット・検査） | 軽 | low〜medium | 規約が決まっていて機械検査がある |
| 明確な人間コメントへの局所修正 | 軽 | low〜medium | 窓の中だけで閉じる。`comment-eater` |
| lint 後の機械的修正・書式変換 | 軽 | low | `--fix` で決まる範囲 |
| Learning Checkpoint の初期候補 | 軽 | medium | 列挙は軽い |
| 転移問題の設計・粒度の判断 | 重 | high | 教材の外へ持ち出せる問いを作れるかは差が出る |
| リンク確認・脚注書式 | 軽 | low | 規約が `citation.md` にある |
| 大規模改稿 | 重 | xhigh（要検証） | 全文横断＋複数制約の同時最適化 |
| blind judge | 重 | high | 判定が割れるとベンチの結論が動く。複数モデルで一致を見る |

## effort について

**高い effort ほど文章品質が上がるとは仮定しない。** medium / high / xhigh を同じ事例で並べて測る。

xhigh に価値があるとまず疑うのは次の3つで、**これ以外では xhigh を既定にしない**。

1. 難しい構成設計（タスクE）
2. 全文横断のレビュー（タスクA）
3. 複数制約の同時最適化（タスクF、大規模改稿）

## 実測（`bench` の結果でのみ書き換える）

| 工程／タスク | 実行ID | 結論 | 根拠 |
|---|---|---|---|
| — | — | **未測定** | — |

書き換えるときの決まり。

- 実行ID（`bench/runs/<id>`）と judge の判定ファイルを添える。**「体感」で書き換えない。**
- 1回の生成結果で書き換えない。同一条件の再生成で再現したものだけを書く。
- 素材が変わった事例の結果を、変わる前の結果と横並びに比べない（`bench.py check` が警告する）。
- 差が出なかったことも書く。**差が無いと分かれば、安い方を選べる。**
