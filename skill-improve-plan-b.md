# やる夫式教材スキル改善案 B

## 1. 結論

提示された簡略ルールへの全面置換は行わない。ただし、現行スキルのコストは「起動時に読む量」ではなく **節ごとに再読する量** が支配的であり、そこを削らない限り削減効果は限定的である。

本案は次の3層に分ける。

1. **散文で書かれた文体・作風の指示**（style-guide、必読サンプル3本、SKILL.md の重複部）は、提示ルールへ統合して削除する。
2. **節カードは廃止せず、圧縮して節ごと再読を維持する**。廃止すると長編後半で規範が失われる。
3. **スクリプト（`yaruo-format`、`check_dialogue_constraints.py`、`yaruo-count`）は一切削らない**。トークンをほとんど消費せず、自然言語では代替できない検出を担う。

改善案 A との主な相違は 2 である。A は節カードの常時参照を廃止して状態メモへ寄せるが、本案は「103節の教材で規範が後半まで保つか」を最大のリスクとみなし、節カードを 2,009字 → 600字程度へ圧縮したうえで再読を残す。

## 2. 現状のコスト構造

### 2.1 ファイル単位

| ファイル | Unicode文字数 | 推定トークン | 読む回数 |
|---|---:|---:|---|
| 常時ロード（各SKILLのdescription 4本） | 約450 | 約500 | 毎セッション |
| `yaruo-rediscovery/SKILL.md` | 7,180 | 約6,800 | 起動時1回 |
| 必読サンプル3本 | 6,641 | 約6,400 | 起動時1回 |
| `references/style-guide.md` | 5,593 | 約5,900 | 初回執筆時1回 |
| `references/section-card.md` | 2,009 | 約2,100 | **節ごとに毎回** |
| `references/anti-patterns.md` | 1,297 | 約1,400 | 随時 |
| `yaruo-format/SKILL.md` | 4,068 | 約3,300 | 整形時1回 |
| `yaruo-proofread/SKILL.md` | 7,126 | 約6,700 | 校正時1回 |
| `yaruo-count/SKILL.md` | 1,347 | 約1,100 | 集計時1回 |

トークン数は日本語・Markdown混在のため概算である。以下では相対比較にのみ用いる。

### 2.2 教材1冊あたりの累計

節数は実測値を用いる（`grep -c '^### '`）。

| 規模 | 実例 | 節数 | 起動＋執筆準備 | 節カード再読 | 合計 |
|---|---|---:|---:|---:|---:|
| 標準 | `weather-forecasting` 級 | 約25〜30 | 約19,100トークン | 約63,000 | **約82,000** |
| 大型 | `philosophy` | 82 | 約19,100 | 約172,000 | **約191,000** |
| 最大 | `chemical-plant` | 103 | 約19,100 | 約216,000 | **約235,000** |

標準教材の本文は3.4万〜5.1万字（約4万トークン）であるから、**指示のトークン量が本文の出力量を上回っている**。

さらに、節カードは再読のたびにコンテキストへ積み上がるため、実コストは上表の単純和より大きい。以降の全ターンが、それまでに積んだ全コピーを入力として払い続ける。

### 2.3 節カードが最大の削減余地である

現行の総指示トークンに占める節カードの比率は、標準教材で約77%、`chemical-plant` で約92%である。したがって、SKILL.md やサンプルをどれだけ圧縮しても、節カードに手を入れなければ全体の削減率は20%程度で頭打ちになる。

一方で、節カード自身の冒頭に廃止できない理由が明記されている。

> 長文では、起動時に読んだ規範がコンテキスト後半で薄れる。このカードは節ごとに読み直す前提で短くしてある。

よって本案は「廃止」ではなく「1回あたりの単価を下げる」方針を採る。

## 3. 提示ルールとの突き合わせ

### 3.1 提示ルールで代替できるもの

| 現行の記載 | 提示ルールの該当箇所 | 判定 |
|---|---|---|
| style-guide のキャラクター定義 | 「登場人物の性格や役割の定義」 | ほぼ同等。やる夫の口調（・・、！！）まで指定されており、むしろ具体的 |
| style-guide の Markdown書式 | 「執筆ルール」「フォーマット」 | 同等。見出し階層、発言表記、TeX、半角数字を網羅 |
| SKILL.md の分量帯表 | 「6〜10章程度、一節2,000〜4,000字」 | 代替可 |
| SKILL.md 冒頭の再発見サイクル要約 | 「素朴な案 → 欠点指摘 → 正解を再発見」 | 骨格は代替可（3.3参照） |
| Step 1 のプロット合意 | 「プロットが完成したら一度確認をとる」 | 代替可 |
| 作風カタログ（完成例7本の表） | なし | 不要。`docs/books/` に56冊あり、必要時に検索すればよい |
| 必読サンプル3本 | なし | 不要。同上 |

削減見込み：style-guide 5,593字 ＋ サンプル 6,641字 ＋ SKILL.md の重複部で、約13,000字。

### 3.2 提示ルールに無く、失うと影響が大きいもの

1. **具体的反例をやる夫自身に判定させる段**。提示ルールは「欠点を指摘され」であり、教師が破綻を宣告する形を許してしまう。現行 SKILL.md はこれを明示的に禁じている。
2. **失敗原因の言語化**。やる夫が「失敗した」ではなく失敗の構造を自分の言葉で述べる段。これが無いと、教師の説明を生徒が承認するだけの形へ退化する。
3. **検算（合格例と不合格例の両方）**。会話上の納得だけで節を閉じない歯止め。
4. **冒頭に全体説明を置かない禁則**。「はじめに」「本書の狙い」を書かせないための指定。提示ルールのフォーマットは登場人物紹介の直後に本文が来る形だが、禁止が明文化されていないと導入節がテーマ紹介になりやすい。
5. **知識予算**。生徒が作中でまだ到達していない知識を突然披露しない制約。
6. **ツアー部分20%以内**。歴史・分派・応用の羅列で再発見サイクルを置き換えない制約。
7. **状態台帳**。定義済み記号、反復トイ例、伏線と回収先、未解決課題、出典URLの追跡。長編で全文再読を避けるための仕組み。
8. **縮約改訂の逆算手順**。「残す節数 × 下限字数」を圧縮の下限として先に提示し、達成できない削減率を約束しない手順。
9. **分割執筆の停止点**。2幕ごとにレビューを求める運用。

### 3.3 数値の矛盾（統合前に決着が必要）

提示ルールは「一節は2,000〜4,000字」、節カードは「会話本文1,200〜1,800字（話者表示と改行を除く）」である。定義域が異なるうえ値も重ならない。

`check_dialogue_constraints.py` の定数が正本であるため、統合時はどちらかへ寄せる必要がある。選択肢は次のいずれか。

- (a) 節カードの値を維持し、提示ルールの数値を書き換える。既存56冊との整合が取れる。
- (b) 提示ルールの数値を採用し、スクリプトの定数を変更する。既存教材が一斉に warning を出すため、実測分布の確認が必要。

本案は (a) を推奨する。

## 4. 提案する構成

### 4.1 `yaruo-rediscovery/SKILL.md`（目標 3,500字）

提示ルールを骨格として採用し、次を追加する。

- 制作フローと停止点（Step 0 / Step 1 のプロット合意 / Step 4 の2幕ごとレビュー / Step 5 の検査コマンド）
- 再発見サイクル6段（3.2 の 1〜3 を含む完全形）
- 教材全体に効く3つの禁則（冒頭の全体説明、第1幕のテーマ紹介、箇条書きだけの演習）
- 知識予算とツアー20%
- 状態台帳の項目
- 縮約改訂の逆算手順

削除するもの。

- 分量帯の3段表 → 提示ルールの章数・節字数で代替
- 作風カタログの表7行 → 「必要なら `docs/books/` から近いものを1〜3節だけ読む」の1文へ
- 「資料の読み方」の表 → 参照先が2つになるため不要
- style-guide への参照全般 → 本文へ統合

### 4.2 `references/section-card.md`（目標 600字）

残すもの。

- 再発見サイクル6段の見出しだけ（本文はSKILL.md側）
- やってはいけない3つ（即答／教師総括／無関係な受け）
- 各節に必ず入れる3つ（速度変化／感情の変化／理解の所有権）
- 1発言7行上限
- 漫才を1節に1〜2回

参照資料へ移すもの（問題が出たときだけ読む）。

- 発言長4帯の表（瞬発・短い推論・展開・錨）
- ボケとツッコミの手札リスト
- 速度変化の具体例
- 数値の正本がスクリプトである旨の説明

これにより節あたり約2,100トークン → 約650トークンとなる。

### 4.3 `references/style-guide.md`

削除し、内容を次へ分配する。

- キャラクター、会話とドラマ、Markdown書式 → 提示ルールへ統合済みとして削除
- 数式・導出の説明（中間段階を省略しない指定） → SKILL.md 本体へ移す。数式中心の教材で効くため
- 参考文献・脚注 → `yaruo-proofread` へ集約（現在2箇所に重複）

### 4.4 サンプル3本

起動時必読を廃止する。ファイル自体は削除せず、`references/` に残して SKILL.md からは「導入がテーマ説明になったとき」「教師の講義が続いたとき」の診断用として参照先だけ示す。

### 4.5 `references/anti-patterns.md`

現状維持。既に1,297字と短く、症状ベースで必要時のみ読む設計になっている。4.2 で節カードから外した発言長帯・漫才手札はここへ統合する。

## 5. スクリプト群の扱い

**いずれも削減対象としない。** 理由は、トークン消費がほぼゼロ（実行して結果だけ読む）であるのに対し、代替するとモデル側に高コストな注意を要求するためである。

### 5.1 `yaruo-count` は削除不可

`scripts/generate_site.py:39-42` が `count_textbooks.py` を直接 import している。

```python
COUNT_SCRIPT_DIR = ROOT / ".claude" / "skills" / "yaruo-count" / "scripts"
...
from count_textbooks import count_document
```

スキルディレクトリを整理する際に、このスキルを移動・削除するとサイト生成が壊れる。SKILL.md 側（1,347字）も既に短く、圧縮余地は小さい。

### 5.2 `yaruo-format`

維持する。SKILL.md 4,068字のうち、CommonMark の flanking 規則の解説など、実行者が理解しなくてよい詳細はスクリプトのコメントへ移せる。SKILL 本文は「`--check` → 適用 → 再`--check` → warning目視 → `git diff` 確認」の5手順へ圧縮できる（目標 1,500字）。

自然言語ルールで代替してはならない。日本語の約物に隣接した `**` の破綻、表の区切り行欠落、TeXブロックの崩れは、執筆時の注意では取りこぼす種類の不具合である。直近のコミット履歴（`fix tex blocks`、`fix 2 texts`）がその実例にあたる。

### 5.3 `check_dialogue_constraints.py`

維持する。脚注の未定義・未使用、節番号の破れ、終端マーカーの揺れ、冒頭の全体説明節、金額・割合表記は、いずれも決定的に判定でき、かつモデルが見落としやすい。

### 5.4 `yaruo-proofread`

維持する。7,126字は圧縮余地があり、Web取得手段の具体的手順は環境変化で陳腐化しやすいため分離してよい（目標 4,000字）。ただし「意図された生徒の誤答を訂正しない」という指定は、この教材形式に固有で他から導けないため必ず残す。

## 6. 期待効果

### 6.1 トークン削減

| 場面 | 現行 | 本案 | 削減率 |
|---|---:|---:|---:|
| 起動時（SKILL＋サンプル） | 約13,200 | 約3,300 | 75% |
| 初回執筆まで（＋style-guide） | 約19,100 | 約3,300 | 83% |
| 節あたりの再読 | 約2,100 | 約650 | 69% |
| **標準教材1冊（30節）** | **約82,000** | **約22,800** | **72%** |
| **`chemical-plant` 級（103節）** | **約235,000** | **約70,300** | **70%** |

参考として、提示ルールのみへ全面置換した場合は約1,000トークン（99%減）となる。本案との差 約22,000トークン（標準教材の場合）が、3.2 で挙げた9項目と長編後半の規範維持に対する保険にあたる。

コンテキストの積み上がりを考慮すると、実際の課金トークン削減率はこれより大きくなる可能性がある。

### 6.2 品質面

期待する改善。

- 中核ルールが短くなり、コンテキスト後半でも参照されやすくなる。
- サンプル3本への作風の過適合が減る。
- 同じ規則が SKILL・節カード・style-guide の3箇所に散在する状態が解消される。
- 書式はスクリプト、内容はモデル、という責任分担が明確になる。

想定リスク。

- 節カードを600字へ削ることで、発言長のばらつきが減り会話速度が単調になる可能性。→ 発言長4帯を anti-patterns へ移すため、症状が出た時点で回復できる設計とする。
- サンプル廃止により、導入節がテーマ説明へ回帰する可能性。→ 「冒頭に全体説明を置かない」禁則を SKILL.md 本体に残すことで抑止する。
- 提示ルールの「一節2,000〜4,000字」をそのまま採用すると、既存教材より節が長くなる。→ 3.3 の (a) で解消する。

## 7. 移行手順

1. 現行スキル一式を基準版として固定する（タグまたはブランチ）。
2. 3.3 の数値矛盾を先に決着させる。ここが未決だと以降の比較が成立しない。
3. 圧縮版 `yaruo-rediscovery` を別ディレクトリへ作成する。スクリプトは一切変更しない。
4. 既存教材のうち、数式中心（`general-relativity`）、社会科学（`local-government`）、CS（`weather-forecasting`）の3テーマについて、プロットと2節を新旧両版で生成する。
5. `check_dialogue_constraints.py` の error / warning 数、および6段の欠落率を比較する。
6. **長編での忘却を必ず検証する**。40節相当まで書かせ、後半10節で「即答」「教師総括」の発生率が前半から悪化しないことを確認する。本案の最大のリスクはここに集中しており、短い試作では検出できない。
7. 合格した場合のみ正本へ反映する。
8. その後、`yaruo-format` と `yaruo-proofread` の SKILL.md を個別に圧縮し、再度検査する。

## 8. 改善案 A との差分

| 論点 | 案A | 案B |
|---|---|---|
| 節カードの節ごと再読 | 廃止し、状態メモで代替 | 600字へ圧縮して維持 |
| style-guide | 3分割して必要時参照 | 削除し、数式部分のみ SKILL へ移動 |
| サンプル3本 | 必読廃止・任意参照 | 同じ |
| 1節の字数 | 1,200〜2,000字を提案 | スクリプト定数（1,200〜1,800字）に合わせる |
| `yaruo-count` | 現状維持 | 現状維持（＋ビルド依存のため削除禁止を明記） |
| 削減目標 | 指示コンテキスト70〜85% | 教材1冊あたり約70% |
| 主要リスクの所在 | 口調・会話速度の単調化 | 長編後半での規範忘却 |

両案は大枠で一致しており、実質的な選択は「節カードの毎節再読を残すか」の一点に絞られる。案Aを採る場合は移行手順6の検証を、案Bより厳しい条件（80節以上）で行うべきである。

## 9. 判断を求める論点

1. 節カードを600字へ圧縮した状態で、80節超の教材の後半でも規範が保たれるか。
2. 提示ルールの「素朴な案 → 欠点を指摘され → 再発見」を、6段（具体的反例・原因の言語化・検算を含む）へ戻す必要があるか。それとも3段で十分か。
3. 1節の字数を、スクリプト定数と提示ルールのどちらへ寄せるか。
4. サンプル3本を廃止した場合、導入節の品質が既存56冊の水準を保てるか。
5. 削除候補とした規則のうち、過去に具体的な失敗を防いだ実績があるものが含まれていないか。
