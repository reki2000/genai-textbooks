# やる夫式教材スキル改善案：Codex・Claude合意

作成日：2026年8月2日

対象：`skill-improve-plan-a.md` と `skill-improve-plan-b.md` の統合

## 1. 結論

ユーザーが提示した簡略ルールを骨格として採用するが、それだけへの全面置換はしない。`yaruo-rediscovery` の教育設計上の中核を短い本体と生成用カードへ圧縮し、決定的処理と内容検証を別スキル・スクリプトへ残す。

目標は、品質を落とさず次を削減することである。

- 起動時に読む長い説明
- 必読サンプル3本
- 同じ規則の複数ファイルへの重複
- 同一コンテキスト内での節カードの重複読み込み
- 実行環境に依存する手順の過剰な説明

初回改善では、既存教材、検査スクリプト、ビルドシステムを一括して変更しない。圧縮版を新旧比較し、合格後に段階的に正本へ反映する。

## 2. 合意事項

### 2.1 `yaruo-rediscovery` 本体

1. `yaruo-rediscovery/SKILL.md` は3,000〜4,000文字を目標に圧縮する。目標値は規範を削るための厳守上限ではない。
2. 次は本体へ残す。
   - 具体的課題、素朴案、具体的反例、原因の言語化、修正、検算の6段階
   - プロット提示後の承認停止
   - 2幕執筆後のレビュー停止
   - 読者とやる夫の知識予算
   - 状態台帳
   - 冒頭に全体説明を置かず、具体的事件から始める禁則
3. 起動時に完成例3本を全文必読とする運用を廃止する。サンプルは削除せず、導入の講義化、数式展開、長編構造など、症状と用途に応じて必要な1〜3節だけ参照する。
4. 人物の行動原理と最低限のMarkdown書式を本体へ統合し、長い人物発言例は削除する。
5. 数式対話に固有の詳細は、数式中心の教材だけが読む条件付き参照へ分離する。

### 2.2 生成用カード

約600字を目標とする短い生成用カードを残す。600字は厳守上限ではなく、次の規範を曖昧にしないことを優先する。

- 6段階の再発見サイクル
- 即答問答の禁止
- 教師総括の禁止
- 無関係な受けの禁止
- やる夫による理解・失敗原因・未解決課題の言語化
- 感情と会話速度の単調な反復の回避

読み込み単位は「節」や曖昧な「セッション」ではなく、本文生成を行うアシスタントターンとする。

- 節を新規執筆する、または会話を大幅改稿する各ターンで、最初の節を書く直前に1回読む。
- 同じターン内で後続節を書くときは再読しない。
- 即答、教師総括、発言偏重などのwarningへ内容修正で対応するときは、修正前に再読する。
- プロットのみのターン、誤字・書式・脚注番号だけの修正、機械的なformat修正では読まない。

これにより、2幕レビュー後やコンテキスト圧縮後の再開を自然にカバーしながら、同一カードのコピーを会話履歴へ重複して積まない。

### 2.3 節と全体の分量

`SKILL.md` から固定の節字数帯を削除する。代わりに次を用いる。

> 1つの中心問題を掘り下げるのに必要な長さだけ書く。短い中心問題は隣接節へ統合し、2つの核心概念が競合する節は分割する。ユーザーが字数を指定した場合はそれに従う。

全体分量帯は作業スコープとコストの見積もりに必要なので維持する。ただし品質基準とはみなさない。分量を満たすためだけに概念、会話、節を増やさない。

現行の `SECTION_CHAR_MIN/MAX` は初回改善では変更しない。ただし字数warningはA/B評価の合否判定から外す。warning閾値の再設計は別作業とする。

### 2.4 状態台帳

本文全体を毎回読み直す代わりに、次を含む状態台帳を維持する。

- 読者の知識予算と現在の到達点
- 定義済みの用語、記号、数値
- 反復トイ例の使用履歴
- 伏線、回収先、未解決課題
- 幕ごとの感情曲線
- 確認済み事実の出典URL

各節の後に更新し、次節ではプロット、台帳、直前の接続部分を優先する。

### 2.5 参考文献規約

`.claude/skills/yaruo-rediscovery/references/citation.md` を短い単一正本として新設する。`yaruo-rediscovery` と `yaruo-proofread` の双方から参照し、同じ規則を複製しない。

字数上限を先に決めず、次を曖昧さなく定義できる最短量とする。

- 見出し名
- 脚注の通し番号と同一ソースの再利用
- Web出典、原典、書籍の最小形式
- 本文のどの主張に対応するかという短い解説
- URLは実在と内容を確認してから記載すること
- 参照日

特定テーマの完成済み脚注を固定テンプレートにしない。

### 2.6 他スキル

- `yaruo-format`：4つの機械修正スクリプトを維持する。SKILL本文は `--check`、適用、再検査、warning確認、diff確認を中心に圧縮する。
- `check_dialogue_constraints.py`：維持する。errorとwarningの区別を保ち、warningを機械的に0へしない。
- `yaruo-proofread`：ファクト、論理展開、数式の検証基準を維持する。意図された生徒の誤答を訂正しない規則、URLの実在・内容確認、数値再計算、修正報告を残す。Webツール固有の冗長な説明は分離・圧縮する。
- `yaruo-count`：実装とSKILLを維持する。初回改善では移動・改名もしない。

### 2.7 description

常時ロードされる各スキルの `description` も、起動判定に必要な語を残して短縮する。削減量は小さいため主要効果として誇張しない。

短縮後は、最低限次の起動回帰試験を行う。

- 新規執筆、続編、部分改訂、縮約改訂で `yaruo-rediscovery` が起動する。
- 整形、会話表記、強調、表、句点に関する依頼で `yaruo-format` が起動する。
- 校正、出典、論理展開、数式検証に関する依頼で `yaruo-proofread` が起動する。
- 概算ではなく正確な文字数・読了時間を求めた場合だけ `yaruo-count` が起動する。

## 3. 測定結果と位置づけ

### 3.1 再現した値

`.claude/skills/yaruo-rediscovery/scripts/check_dialogue_constraints.py` の `parse_sections` を使用し、`docs/books/*/README.md` 56冊を集計した。

| 集計条件 | 件数 | 中央値 | 1,200〜1,800字に入る割合 |
|---|---:|---:|---:|
| 話者を含む全見出し | 1,663 | 1,111字 | 28.7% |
| `should_check()` が真 | 1,556 | 1,094字 | 29.5% |

「話者を含む全見出し」には `##` 見出し133件が含まれる。検査スクリプトが実際に会話量検査の対象とする主値は、`should_check()` が真となる1,556件である。

### 3.2 測定値を規範にしない

既存教材には品質が良いものと悪いものが混在するため、この分布から理想の節字数や品質基準を決めない。

測定値は、次の参考に限る。

- 現行の字数warningが多数の既存節へ出る。
- 教材ごとに節の粒度が大きく異なる。
- 字数warning単独では新旧品質の判定材料にならない。

したがって「既存教材が多く採用しているから正しい」「現行帯に入らないから悪い」とは判断しない。

## 4. 凍結・依存関係

初回改善では次の依存関係を維持する。

1. `scripts/generate_site.py` は `.claude/skills/yaruo-count/scripts/count_textbooks.py` をパス指定でimportする。`yaruo-count` のディレクトリ移動・改名・削除を行わない。
2. 本改善で `yaruo-proofread/SKILL.md` から `.claude/skills/yaruo-rediscovery/references/citation.md` へのクロススキル参照を新設する。この参照先を単独で削除・移動しない。

将来スキルを単体配布する場合は、クロススキル参照を解消する必要がある。`yaruo-count` を移動する場合は、先にビルドコードとの結合を解消する。

## 5. 評価計画

改善案は、同じ課題を現行版と圧縮版へ与えるA/B比較で評価する。評価者には可能な限りどちらが現行版かを伏せる。

### 段階1：短いA/B

数式、社会科学、CS・工学の3分野について、プロットと2節程度を新旧で生成する。

評価する。

- 6段階の充足
- 素朴案が藁人形でなく、一見もっともらしいか
- 具体的反例をやる夫自身が判定したか
- 失敗原因をやる夫が言語化したか
- 元の反例と別例で検算したか
- 教師の講義を分割しただけになっていないか
- 数式・論理を初学者が追えるか
- キャラクターとドラマが内容に結び付いているか
- 機械的errorの件数
- warningカテゴリごとの現行版との差。ただし字数warningは除く

### 段階2：長編途中からの再開

既存長編のプロット、直前部分、状態台帳を与え、第35節相当から再開する。次の2条件を比較する。

1. 完全な台帳
2. 直近5節分、伏線、または未解決課題の一部を意図的に欠落させた劣化台帳

評価対象を次へ限定する。

- 即答問答
- 教師総括
- 知識予算超過と未定義記号
- 直近の未回収課題
- 台帳の不足を検知し、既存本文を検索して補えるか

「補えた」は最低限、次で判定する。

- 欠落区間で定義された記号・用語を、再開後の本文で無定義に使わない。
- 欠落区間で提示された未解決の問いを、再開後10節以内に回収するか、未回収として台帳へ戻す。

### 段階3：限定的な連続試作と実運用

段階1・2を通過した候補だけ、10〜20節程度の連続試作へ進める。その後、実教材2〜3件で観測する。

評価する。

- 幕をまたぐ感情の大弧
- 同じ慢心・転落の過剰反復
- 反復トイ例の育成
- 歴史・分派・応用ツアーの過剰化
- 状態台帳更新の長期的な脱落
- 後半での生成規範の忘却

### 合否基準

- 機械的errorは絶対値0とする。
- warningを0件にすることは目的にしない。
- 字数warningは合否から除外する。
- 即答、教師総括、発言偏重、承認語など、その他のwarningはカテゴリ別に現行版との差を比較する。
- 重大な事実誤認、成立しない反例、正解として提示された数式の誤りを増やさない。
- 6段階の欠落率を現行版から有意に悪化させない。
- ブラインド評価で、教育的な再発見感と読み物としての品質を現行版と同等水準に保つ。

評価件数が少ない段階では統計的有意差を主張せず、重大な失敗の有無と劣化傾向を判断する。

## 6. トークン効果の報告方法

単一の削減率や確定トークン数を先に約束しない。最低限、次を分けて報告する。

| シナリオ | 読み込みモデル |
|---|---|
| 低コスト | 1つの本文生成ターンでカードを1回読み、複数節を書いても再読しない |
| 標準 | 2幕レビューなどにより、教材制作中に複数の本文生成ターンがある |
| 上限比較 | 旧運用を文字どおり解釈し、節ごとにカード全文を新規ロードする |

次を別々に扱う。

- 起動時の固定入力
- 初回本文執筆までの入力
- 本文生成ターン数 × カード単価
- 後続ターンで会話履歴が再送される量
- プロンプトキャッシュ適用前後の課金
- コンテキスト圧縮の有無
- 本文生成、調査、校正を含む制作全体

実測できる環境ではAPI usageを使用する。実測できない場合は、文字数と読み込み回数から上下限を示し、上限シナリオを既定値として提示しない。

確実に期待できる方向性は次である。

- 起動時の必読サンプル3本をなくすため、固定入力が減る。
- 長いstyle-guideを分解するため、不要な規約を読まなくて済む。
- 生成用カードの単価が下がる。
- 同一ターン内の重複読み込みがなくなる。
- description短縮により、起動しないターンの固定入力も少量減る。

## 7. 実装順序

1. 現行スキル一式を基準版として固定する。
2. 圧縮版 `yaruo-rediscovery` と生成用カードを別ディレクトリまたは別ブランチへ作る。
3. サンプルを任意参照へ変更し、数式規約と脚注規約を分離する。
4. 既存の検査スクリプトを変更せず、段階1・2のA/B評価を行う。
5. 合格した場合だけ圧縮版を正本へ反映する。
6. `yaruo-format`、`yaruo-proofread`、descriptionを個別に圧縮し、各変更後に回帰試験する。
7. 段階3の連続試作と実教材で最終確認する。
8. warning閾値の再設計が必要なら、良質と人手評価された教材を基準に別作業として着手する。

## 8. 本改善のスコープ外

次は先送りではなく、効果を分離して測るため意図的に別作業とする。

- `SECTION_CHAR_MIN/MAX` を含むwarning閾値の再設計
- 既存教材の品質評価と一括修正
- スキルの単体配布への対応
- `yaruo-count` とビルドコードのパス依存解消
- サイト生成仕様とカタログ構造の変更
- 本改善と同時の検査スクリプト全面改修

## 9. 最終判断

採用するのは、案Aの「状態台帳と段階評価」と案Bの「短い生成用カードで長編後半の規範を守る」という懸念を統合した案である。

生成用カードは毎節ではなく各本文生成ターンに1回読み、コンテキストへ同じ全文を重複追加しない。節字数は固定せず、内容とユーザー指定に従う。サンプルは必読から任意参照へ変え、書式・校正・集計の決定的処理は維持する。

これにより、現行スキルの独自性である再発見型の教育設計を残しながら、固定入力、重複参照、条件に合わない資料の読み込みを削減する。
